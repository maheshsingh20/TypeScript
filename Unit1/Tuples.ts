/*
let tup: [string, number] = ["Hello", 45];
let [first, second] = tup;
console.log(first);
console.log(second);
*/

let myTuple: [string, boolean, number, string] = ["Mahesh Singh", true, 234, "Delhi"];
let [first, second, third, fourth] = myTuple;